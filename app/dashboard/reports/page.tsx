import { ComingSoon } from "@/components/ComingSoon";

export default function ReportsPage() {
  return (
    <ComingSoon
      title="Reports"
      description="Deeper reporting across loads, revenue, driver pay, and everything the later phases add — beyond what fits on the Dashboard."
      phase="a future phase"
    />
  );
}
