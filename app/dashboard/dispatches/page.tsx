import { ComingSoon } from "@/components/ComingSoon";

export default function DispatchesPage() {
  return (
    <ComingSoon
      title="Dispatches"
      description="A dispatch is the operational side of a load — driver, truck, routing, check calls, and trip sheet, split out from the commercial booking. Today, Loads covers both."
      phase="Phase 3 (the load/dispatch split)"
    />
  );
}
